name: Docker Workflow

on:
  push:
    branches:
      - ts_js

jobs:
  build-and-push:
    name: Build & Push Docker image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        run: |
          docker build -t my-nextjs-app-ts-js .
          # docker run -d -p 3000:3000 my-nextjs-app-ts-js
          docker tag my-nextjs-app-ts-js sreedockerhub19/my-nextjs-app-ts-js:latest
          docker push sreedockerhub19/my-nextjs-app-ts-js:latest